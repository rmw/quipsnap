<%= render partial: "quotes/search" %>

<div class="quotes">
	<ul>
		<% @quotes.each do |quote| %>
		<div class="quote">
			<div>Content: <%= quote.content %></div>
			<div>Title: <%= quote.title %></div>
			<div>Author: <%= quote.author %></div>
			<div>Created by: <%= quote.user.username %></div>
		</div>
		<% end %>
	</ul>	
</div>